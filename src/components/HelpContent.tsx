const HelpContent = () => {
  return (
    <div>
      <h2>How To Play</h2>
      <p>
        Move all cards to the foundation piles by suit in ascending order. Use
        free cells to hold cards temporarily. Stack tableau columns in
        descending order with alternating colors.
      </p>
    </div>
  );
};

export default HelpContent;
